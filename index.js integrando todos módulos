const pool = require('./db');

async function testarConexao() {
  try {
    const res = await pool.query('SELECT NOW()');
    console.log('Banco conectado! Data atual:', res.rows[0]);
  } catch (err) {
    console.error('Erro ao conectar no banco:', err);
  }
}

testarConexao();
